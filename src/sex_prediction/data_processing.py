import pandas as pd

def load_data(file_path):
    # Load data from file_path (name frequency and transcriptions).

    # Create total frequency column in name frequency data.

    #
    return pd.read_csv(file_path)